FINAL HOMEPAGE POLISH

Copy/merge this package into the existing `niv-portfolio 2` folder.

Fixed:
- Removed the duplicate monitor-screen SVG that caused ghosted/doubled text and rays.
- Symmetrized the three orange rays above “Ready to Explore?”.
- Symmetrized the left and right decorative rays around the View My Work button.
- Restored a continuous, faster, top-anchored swing for the supplied straight bulb.
- Kept the character slightly inward/left on the books.
- Removed the blue glow treatment; hover now subtly scales the real monitor content and button without rendering a duplicate.
- Kept the monitor/button unlinked.
- Kept the temporary second page and walking character out of the active site.
- Preserved the archived walking-character and old-page code for future reuse.
- Updated the embedded illustration fallback so direct HTML and localhost render the same artwork.
